enum ReadingTheme { light, dark }

enum ReadingOrientation { portrait, auto, landscape }

enum ReadingSpacing { compact, normal, relaxed }

enum ReadingIndent { none, small, large }

enum ReadingMode { scrolling, paging }
